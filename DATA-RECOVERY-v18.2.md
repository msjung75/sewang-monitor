# 기존 자료 표시 회귀 복구

계정별 로컬 저장을 도입하면서 기존 소유자 정보 없는 저장 키의 복구 경로가 빠졌다. 원본 서버 파일과 기존 localStorage 키는 삭제되지 않았지만 추적 매장·메모·거래처가 비어 보일 수 있었다. 대용량 파일 로드 실패도 화면에서 실제 0건과 구분되지 않았다.

- 이전 기기 기록을 발견하면 로그인한 직원에게 `이 기기의 내 기록 복구` 버튼을 표시한다. 본인 기록임을 확인하는 버튼으로 추적 매장(v3 포함), 메모, 거래 브랜드, 거래처 매장, 도매사 표시, 방문 기록을 계정 공간에 합친다.
- 원본 키를 삭제하거나 서버로 전송하지 않는다. 동일 항목은 현재 계정 기록이 우선한다. 복구 완료 표시로 재실행과 다른 계정의 재가져오기를 막는다. 알려진 다른 소유자, 미승인 사용자 및 업주는 영업 기록을 가져올 수 없다.
- 파싱 또는 저장공간 오류는 실패로 표시하고 불완전한 대상 쓰기를 되돌린다. 자동 캐시 수 MB를 복제하는 대신 재생성 불가 영업 기록을 우선 저장한다.
- 방문 기록과 거래처 인덱스는 로그인·복구 뒤 다시 읽는다. 복구 전 첫 방문/미거래 판정이 고정되던 초기화 순서도 수정했다.
- 정부 누적 자료는 권한 검사를 유지한 작은 JSON 조각으로 전송한다. 완전성 검증 후에만 적용하며, HTTP 압축 헤더가 프록시에서 바뀌어도 전송할 수 있다.
- 로딩·실패·재시도를 모든 탭 상단에 표시한다. 새 영업 화면에서는 아직 불러오지 못한 자료를 0건 KPI로 표시하지 않는다. 이미 읽은 자료는 재시도 실패로 비우지 않는다.
- 서비스 워커 갱신 메시지에서 과거 `forceClear/clearKeys` 필드를 제거했다. 사용자 브라우저 기록을 초기화하지 않는다.

검증: 복구 병합/충돌/구형 키/계정 경계/저장 실패 롤백, 버튼 클릭부터 거래처 뱃지와 방문 기록 갱신까지 DOM 통합, 실제 인허가·누적 파일 전송과 한글 보존, 실패/누락/배포 revision 변경, 실제 API 핸들러의 승인·권한 취소·익명 조각 접근 차단. 운영 사용자 세션 및 해당 사용자의 기기 저장공간은 로컬 테스트 대상이 아니므로 실제 개인 기록 복구는 같은 기기에서 로그인 후 버튼을 눌러 완료한다.
