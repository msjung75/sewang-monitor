# 세왕 영업 인텔리전스 v18.1

## 구현 범위

- 영업 홈에 신규 매장 영업 / 창업 트렌드 / 신규 브랜드 유치 / 기존 브랜드 영업 작업공간 추가.
- 기존 대시보드·차트는 접을 수 있는 영역으로 보존. 기존 신규업장, 지도, 브랜드 상세, 관리자 기능 유지.
- 신고업태와 실제업태 추정 구분. 사용자 현장 확인, 오픈 상태, 담당자, 다음 연락일, 영업 단계 기록.
- 네이버 상호 포함 여부만으로 판정하던 로직을 주소·지역·층·호수·후보 점수 차이 검사로 대체.
- 브라우저 재확인과 야간 수집이 동일한 `assets/intelligence-core.js` 규칙 사용.
- API 오류 / 결과 없음 / 복수 후보 / 기존 판정 재검증 / 동일 매장 유력 상태 분리.
- 네이버 정확도순 검색, 요청당 최대 5개 후보, 서로 다른 두 검색어까지 사용.
- 기존 등록 영구 고정·60일 영구 잠금 해제. 0·3·7·14·30·60일 기준 재확인, 이후 180일까지 월 단위 재확인.
- 기존 GitHub Actions 일정 유지. 요청 수를 일자별 기록하여 여러 재시도 실행의 예산을 합산. 인증·권한·호출 제한 오류 시 중단.
- 최근/직전 90일 인허가, 수도권 관측, 수집 표본 내 지역 이동 순서, 날짜 있는 폐업 기록 집계.
- 완료된 두 달만 트렌드 비교. 진행 중인 달과 지난달 전체를 직접 비교하지 않음.
- 설치형 앱의 화면 방향을 `any`로 변경. 720px 미만 단일 열, 720px 이상 목록·상세 동시 표시, 861px 이상 사이드 메뉴, 1450px 이상 신규 매장 3열.
- CSS만으로 화면 크기에 반응하므로 회전·접기·펼치기 시 선택, 검색, 입력 중 메모를 재생성하지 않음.

## 정확도 원칙과 미구현 항목

1. 규칙 점수는 확률이 아니다. `동일 매장 유력`도 현장 확인 확정과 다르다.
2. 검색 결과 없음은 미등록·미오픈 증명이 아니다. 인허가일과 리뷰일로 개업일을 확정하지 않는다.
3. 네이버 지역검색 API의 `telephone`은 현재 값을 반환하지 않는다. 전화는 기존 인허가 연락처만 표시한다.
4. 구글은 비교 검색 링크만 제공한다. Google Places 자동 수집, 유료 API·추가 자격증명은 연결하지 않았다.
5. 업태 분류는 설명 가능한 키워드 규칙이다. 검증된 AI 정확도·학습 완료·서울 진출 확률을 주장하지 않는다.
6. 사용자 확인 기록은 현재 브라우저의 계정별 영역에 저장한다. 팀 공유 DB, 여러 기기 동기화, 자동 학습은 별도 단계다. 브라우저 저장은 암호화된 서버 저장을 대신하지 않는다. 계정 정보가 없는 구버전 자료를 새 사용자에게 자동 배정하지 않는다.
7. 브랜드 거래 기록은 전 지점 거래 확인이 아니다. 미등록을 미거래로 단정하지 않는다. 지점별 세왕 거래율·예상 매출은 계산하지 않는다.
8. YTD는 기존 수집·제외 조건이 적용된 인허가 표본이다. 전체 점포망, 전체 시장 순증/생존율/점유율로 표시하지 않는다.
9. 카카오 승인 역할을 서버 API에도 적용한다. 승인 취소·차단·역할 변경은 JWT에 남은 과거 역할보다 현재 명단을 우선한다. 보안 변경으로 기존 세션은 다시 로그인해야 한다.
10. API 운영 키로 실제 수집을 실행하거나, 라이브 영업 데이터·고객 기록·사용자 권한을 변경하지 않았다.

## 확인 방법

```sh
npm ci
npm test
node tests/preview-server.cjs
```

`tests/preview-server.cjs`는 루프백에서만 구동되는 가짜 데이터 테스트 서버다. 실제 로그인·실제 API를 호출하지 않으며 프로덕션 배포 서버로 사용하지 않는다. `/frames`에서 390/768/1280px iframe 크기로 변경할 수 있다.

자동 테스트는 매칭, 일정, 중복 집계, DOM 화면 구성/검색/저장/크기변경 상태 유지, 전체 HTML 초기화 및 미로그인·승인대기 게이트를 점검한다. DOM 테스트는 실제 픽셀 렌더링 검증을 대신하지 않는다.

## 다음 검증

- 승인된 계정으로 실제 배포의 모바일·폴더블·태블릿 화면 및 기존 지도/브랜드 상세 확인.
- 야간 작업 1회 이후 새 `version: 2` 캐시 생성 및 요청 예산 확인.
- 담당자가 확인한 정답 표본으로 오매칭률과 놓친 매장 비율을 각각 측정해 임계값 조정.
- 보안 운영 조건과 남은 제한은 `SECURITY-DEPLOYMENT.md` 참고.

## 공식 데이터 설명

- 네이버 지역검색 API: https://developers.naver.com/docs/serviceapi/search/local/local.md
- Google Places Text Search: https://developers.google.com/maps/documentation/places/web-service/text-search
