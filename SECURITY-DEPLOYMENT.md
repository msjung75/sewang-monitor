# 보안 변경 및 배포 조건

## 적용한 보호

- 모든 데이터·검색 API는 서버에서 서명된 세션과 현재 승인 명단을 확인한다. 명단 조회 실패는 접근 거부로 처리한다. 관리자 API, 업주용 화면, 직원용 고객 자료 접근을 분리했다.
- 예측 가능한 JWT 대체 비밀값과 빈 명단의 첫 방문자 관리자 승격을 제거했다. JWT는 HS256, issuer, audience, 만료를 검사한다. 기존 세션은 다시 로그인해야 한다.
- OAuth 로그인은 10분 유효한 무작위 state와 HttpOnly/Secure/SameSite 쿠키를 대조한다. 관리자 변경, 로그아웃, 유료 수집은 POST와 정확한 Origin 및 JSON 요청을 요구한다.
- 유료 Apify 수집은 관리자에게만 허용한다. 결과 조회는 서버가 확인한 run의 데이터셋을 사용한다. Push 주소는 지원 제공업체 HTTPS 주소만 허용하고, 구독 해제는 자기 구독에 한정한다.
- 정적 배포 폴더에는 명시한 브라우저 자원만 복사한다. `/data/`는 인증된 서버의 파일 허용 목록을 통하며 사용자·활동 자료는 관리자만 읽는다. Push 구독 파일은 직접 반환하지 않는다.
- 인증된 응답에 공유 CDN 캐시를 사용하지 않으며 wildcard CORS를 제거했다. iframe 삽입, object 삽입, base URL 변경을 제한하는 헤더를 추가했다.
- 로그인 안내와 기존 이벤트 문자열에 들어가는 외부 이름을 이스케이프한다. 활동 로그의 사용자·역할은 클라이언트 입력 대신 검증된 세션에서 가져온다.
- 사용자별 브라우저 저장 영역을 분리하고 로그인 전에는 읽지 않는다. 기존 소유자 없는 로컬 자료는 삭제하지 않고 자동 이관도 하지 않는다.
- 사용자·고객·구독 기록을 GitHub에 쓰기 전에 저장소가 private인지 검증한다. 공개 또는 확인 불가 시 쓰지 않는다. 활동 기록의 GitHub 전송도 공개 상태에서는 중단한다.

## 반드시 남아 있는 운영 조치

1. `msjung75/sewang-monitor`는 점검 시 공개 저장소였다. 기존 사용자 관리 파일, 활동 기록, 고객 매칭 자료 및 과거 커밋은 웹 앱의 로그인만으로 보호되지 않는다. 저장소를 비공개로 전환해야 한다. GitHub 연결에는 이 설정을 바꾸는 도구가 제공되지 않았다. 파일을 최신 커밋에서 지우는 것만으로 과거 공개 기록이 지워지지 않으므로 기록 삭제나 강제 history rewrite는 수행하지 않았다.
2. Vercel에 충분히 긴 무작위 `JWT_SECRET`(최소 32바이트), `KAKAO_REST_KEY`, 현재 저장소에 접근할 `GITHUB_TOKEN`이 필요하다. 첫 관리자 자동 승격은 없으며 `ADMIN_KAKAO_ID` 또는 명단의 기존 관리자가 필요하다. 비밀값 자체를 코드·보고서에 기록하거나 새로 공개하지 않는다.
3. 기존 카카오 JS 키는 공개용 브라우저 키이며 REST 비밀키와 다르다. 카카오 콘솔의 웹 도메인 제한은 운영자가 확인해야 한다. 실제 키 유출 여부를 저장소 전체 과거 이력까지 조사한 것은 아니다.
4. 승인된 실제 계정의 카카오 왕복 로그인, 관리자 저장, 모바일·폴더블 실제 픽셀, 운영 Naver API 수집은 아직 별도의 실서비스 확인이 필요하다. DOM 테스트 통과는 이를 대신하지 않는다.

## 성능과 보안 범위

- 기존 누적 JSON은 약 33MiB로 함수 응답 한도를 넘는다. 인증 뒤 gzip으로 전송하며 점검 시 약 2.98MB였다. 바이너리 응답 포장 여유를 포함해 압축 응답을 3.3MB로 제한한다. 데이터 증가 시 분할 API/저장소 전환이 필요하며 한도 초과는 오류로 드러낸다.
- 승인 명단을 요청마다 읽으므로 GitHub API 장애/호출 한도 도달 시 보호된 기능은 일시 중단된다. 중앙 세션/권한 저장소로 옮기면 이를 개선할 수 있다.
- 브라우저 계정 분리는 화면에서 다른 계정의 자료를 잘못 읽는 것을 막는다. 기기 관리자나 같은 브라우저의 개발자 도구를 막는 암호화가 아니다. 기밀 CRM 자료는 업무용 개인 기기에서 사용하고, 다음 단계에서 권한 검사가 있는 서버 저장소로 이전한다.
- 이번 검사는 특정 기존 취약 경로와 변경 코드에 대한 보완이다. 독립 침투시험이나 앱 전체 XSS 부재 인증이 아니다. 전체 CSP의 script-src 제한과 외부 스크립트 무결성, 분산 요청 제한은 후속 과제다.

## 검증

`npm test`, `npm run build`, `npm audit --omit=dev`, `git diff --check`.

테스트는 익명·차단·승인 취소·역할 하향·권한 저장소 장애·JWT 변조·OAuth state·CSRF·허용되지 않은 파일 경로·공개 저장소 쓰기 차단·계정별 로컬 저장·문자열 삽입·대용량 JSON 전송을 포함한다. 테스트는 가짜 인증 및 데이터를 사용하며 실제 알림/유료 수집을 실행하지 않는다.

공식 배포 사양: https://vercel.com/docs/project-configuration/vercel-json
함수 응답 한도: https://vercel.com/docs/functions/limitations
