# 브랜드 목록과 자동 수집 복구

운영 화면에서 올해 누적 집계는 보이지만 브랜드 목록이 비어 있는 문제를 재현했다.

1. 최근 수집본은 2026-09-16 21:50 UTC 실행에서 17개 지역 수집이 모두 실패한 빈 파일로 덮였다. 사용자 인증이 필요한 앱 API를 로그인 없이 호출하던 기존 수집 작업이 보안 변경 뒤 차단된 상태였다. 이전 스크립트가 실패를 0건으로 변환해 저장한 것도 원인이다.
2. `renderFr()`는 누적 자료가 있어도 최근 30일 `BRANDS`가 비어 있으면 먼저 종료했다. 올해 누적 브랜드 목록과 최근 수집 목록의 의존성을 제거했다.

복원한 `trend30_all.json`은 Git 기록 `89dec09`의 원본과 동일한 blob이다. 수집시각 2026-09-16 14:37:37 UTC, 6,127건, 실패 지역 0개. 수집일을 현재로 바꾸지 않았으며 다른 기존 원본은 건드리지 않는다.

자동 수집은 기존 GitHub Actions 정부 API 비밀키 `DATA_GO_KR_KEY`로 정부 원본을 직접 읽는다. 브라우저 API는 계속 승인 사용자 인증을 요구한다. 인증키·사용자 쿠키를 로그나 결과물에 넣지 않는다. 공통 정부 응답 변환기를 앱과 수집기가 사용하며 HTTP/응답 형식 오류, 서비스 일부 실패, 페이지 한도 초과를 성공한 빈 결과와 구분한다.

17개 지역 전체 검증 뒤에만 최근 스냅샷을 교체한다. 부분·전체 실패는 작업 실패로 끝나고 기존 파일을 보존한다. 누적 수집도 월별 파일을 쓰기 전에 전체 응답을 검증한다. 관련 추천·브랜드·폐업 수집 역시 사용자 로그인에 의존하지 않도록 연결했다. 메인 반영 시 최근 인허가 수집을 한 번 실행하도록 경로 제한 push 트리거를 추가했다.

검증은 사용자 화면의 회귀 재현, 최근 자료 없는 상태에서 실제 누적 자료의 브랜드·주점 필터·지점 상세 표시, 실패 스냅샷에 대한 기기 자료 보존, 정부 응답 오류·페이지 한도, 셸 수집기 실패 시 기존 파일 바이트 보존을 포함한다. 운영 자동 수집 결과는 작업 로그/생성된 스냅샷으로 따로 확인한다.
